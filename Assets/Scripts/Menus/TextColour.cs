using UnityEngine;
using System.Collections;

public class TextColour : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//GetComponent<Text>().color = new Color (Random.Range (0.01f, 0.99f), Random.Range (0.01f, 0.99f), Random.Range (0.01f, 0.99f), 1);

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}